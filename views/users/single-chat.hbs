{{!-- <style>
    .chat-window {
        height: 100vh;
        display: flex;
        flex-direction: column;
    }

    .chat-messages {
        flex: 1;
        overflow-y: auto;
        padding: 20px;
    }

    .chat-input {
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        padding: 10px;
        background-color: white;
        border-top: 1px solid #ccc;
    }

    .header {
        --background-color: rgba(0, 0, 0, 0);
        --default-color: #ffffff;
        --heading-color: #ffffff;
        color: var(--default-color);
        background-color: black;
        padding: 20px 0;
        transition: all 0.5s;
        z-index: 997;
    }
</style>
<br><br><br><br><br>
<div class="container-fluid chat-window">
    <div class="chat-messages">

        <div class="row">
            <div class="col-lg-6">
                <div class="card p-3">
                    <h5>Your Message : </h5>
                    <hr>
                    {{#each chats}}
                    <div class="message">
                        <p><strong>{{this.name}}</strong> : {{this.msg}} </p>
                    </div>
                    {{/each}}
                </div>
            </div>
            <div class="col-lg-6">
                <div class="card p-3">
                    <h5>Tutor's Message : </h5>
                    <hr>
                    {{#each reply}}
                    <div class="message">
                        <p><strong>Tutor</strong> : {{this.msg}} </p>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>




    </div>

    <form action="/add-chat" method="post">
        <input type="hidden" name="tutorId" value="{{tutor._id}}" />

        <div class="chat-input">
            <div class="input-group">
                <input type="hidden" name="userId" value={{user._id}}>
                <input type="hidden" name="name" value={{user.Fname}}>
                <input type="text" class="form-control" name="msg" placeholder="Type your message..."
                    aria-label="Type your message">
                <button class="btn btn-primary" type="submit">Send</button>
            </div>
        </div>
    </form>
</div> --}}


<!-- Chatra {literal} -->
<style>
    /* Center the Chatra widget on the screen */
    .chatra {
        position: fixed !important;
        bottom: 50% !important;
        right: 50% !important;
        transform: translate(50%, 50%);
        z-index: 9999;
        /* Ensures the widget is on top of other elements */
    }
</style>

<div class="container-fluid">
</div>


<script>
    (function (d, w, c) {
        w.ChatraID = '3LZ4smWtAAs8rH3wP';

        // Define user data
        w.ChatraSetup = {
            // Set visitor name and email if logged in
            // These values will pre-fill Chatra’s form fields if required
            name: '{{user.Fname}}',  // User's name from the session
            email: '{{user.Email}}' // User's email from the session
        };

        var s = d.createElement('script');
        w[c] = w[c] || function () {
            (w[c].q = w[c].q || []).push(arguments);
        };
        s.async = true;
        s.src = 'https://call.chatra.io/chatra.js';
        if (d.head) d.head.appendChild(s);
    })(document, window, 'Chatra');
</script>